langcode: en
status: true
dependencies:
  config:
    - group.type.provider
  module:
    - user
id: provider-group_membership
label: 'Provider: Group membership'
description: 'Adds users to groups as members.'
group_type: provider
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
